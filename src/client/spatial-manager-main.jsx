import React from 'react';
import ReactDOM from 'react-dom/client';
import SpatialManagerApp from './components/SpatialManagerApp.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SpatialManagerApp />
  </React.StrictMode>
);